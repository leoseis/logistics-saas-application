import { type ReactNode, useEffect, useState } from 'react'
import { Bike, Box, CircleDollarSign, PackageCheck, Store, TrendingUp, UserCheck } from 'lucide-react'
import { getDashboardAnalytics, type ApiOrderStatus, type DashboardAnalytics as Analytics } from './api'
import { Badge, Card } from './components'
import type { Status } from './types'

const money=(value:string)=>new Intl.NumberFormat('en-NG',{style:'currency',currency:'NGN',maximumFractionDigits:0}).format(Number(value))
const number=(value:string)=>new Intl.NumberFormat('en-NG',{maximumFractionDigits:2}).format(Number(value))
const statusLabel:Record<ApiOrderStatus,Status>={pending:'Pending',assigned:'Assigned',picked_up:'Picked up',delivered:'Delivered',cancelled:'Cancelled'}
const shortDate=(value:string)=>new Intl.DateTimeFormat('en-NG',{month:'short',day:'numeric'}).format(new Date(`${value}T00:00:00`))

export function DashboardAnalytics(){
 const [data,setData]=useState<Analytics|null>(null);const [loading,setLoading]=useState(true);const [error,setError]=useState('')
 useEffect(()=>{const controller=new AbortController();setLoading(true);setError('');getDashboardAnalytics(controller.signal).then(setData).catch(err=>{if(err.name!=='AbortError')setError('Unable to load dashboard analytics. Refresh to try again.')}).finally(()=>{if(!controller.signal.aborted)setLoading(false)});return()=>controller.abort()},[])
 if(loading)return <div className="dashboard-state">Loading business analytics…</div>
 if(error)return <div className="api-error dashboard-error">{error}</div>
 if(!data)return null
 const statuses=[['Pending',data.pending_orders],['Assigned',data.assigned_orders],['Picked up',data.picked_up_orders],['Delivered',data.delivered_orders],['Cancelled',data.cancelled_orders]] as const
 const maximum=Math.max(...data.revenue_trend.map(item=>Number(item.revenue)),1)
 return <div className="analytics-dashboard"><div className="analytics-heading"><div><p className="eyebrow">Live business overview</p><h1>Dashboard</h1><p>Revenue is earned only from delivered orders. Expected revenue is shown separately.</p></div><span className="live-indicator">Live backend data</span></div><div className="analytics-metrics"><Metric icon={<Box/>} label="Total Orders" value={String(data.total_orders)}/><Metric icon={<PackageCheck/>} label="Delivered Orders" value={String(data.delivered_orders)}/><Metric icon={<Store/>} label="Active Vendors" value={String(data.active_vendors)}/><Metric icon={<UserCheck/>} label="Available Riders" value={String(data.available_riders)}/><Metric icon={<CircleDollarSign/>} label="Delivered Revenue" value={money(data.delivered_revenue)} financial/><Metric icon={<TrendingUp/>} label="Average Order Value" value={money(data.average_order_value)} financial/></div><div className="analytics-grid"><Card className="analytics-card revenue-card"><div className="analytics-card-title"><div><h2>Revenue overview</h2><p>Delivered revenue over the last 7 days</p></div><CircleDollarSign/></div><div className="revenue-values"><div><span>Earned</span><strong>{money(data.delivered_revenue)}</strong></div><div><span>Expected, not earned</span><strong>{money(data.pending_revenue)}</strong></div><div><span>Average order value</span><strong>{money(data.average_order_value)}</strong></div></div><div className="revenue-chart" aria-label="Delivered revenue for the last 7 days">{data.revenue_trend.map(item=><div className="chart-column" key={item.date} title={`${shortDate(item.date)}: ${money(item.revenue)}`}><span>{Number(item.revenue)>0?money(item.revenue):''}</span><div className="chart-track"><i style={{height:`${Math.max(Number(item.revenue)>0?8:0,Number(item.revenue)/maximum*100)}%`}}/></div><small>{shortDate(item.date)}</small></div>)}</div></Card><Card className="analytics-card status-card"><div className="analytics-card-title"><div><h2>Order status</h2><p>{data.total_orders} orders across the delivery lifecycle</p></div><PackageCheck/></div><div className="status-overview">{statuses.map(([label,count])=><div key={label}><Badge status={label}/><strong>{count}</strong></div>)}</div><div className="weight-overview"><span>Total active weight <strong>{number(data.total_weight_kg)} kg</strong></span><span>Delivered weight <strong>{number(data.delivered_weight_kg)} kg</strong></span><span>Average weight <strong>{number(data.average_order_weight_kg)} kg</strong></span></div></Card></div><Card className="recent-orders-card"><div className="analytics-card-title"><div><h2>Recent orders</h2><p>Latest shipment activity from Django</p></div><Bike/></div>{data.recent_orders.length===0?<div className="analytics-empty">No orders have been created yet.</div>:<div className="table-wrap"><table className="recent-orders-table"><thead><tr><th>Reference</th><th>Vendor</th><th>Recipient</th><th>Rider</th><th>Weight</th><th>Delivery fee</th><th>Status</th><th>Created</th></tr></thead><tbody>{data.recent_orders.map(order=><tr key={order.id}><td><strong>{order.reference}</strong></td><td>{order.vendor}</td><td>{order.recipient}</td><td>{order.assigned_rider||'—'}</td><td>{order.weight_kg?`${number(order.weight_kg)} kg`:'—'}</td><td>{money(order.delivery_fee)}</td><td><Badge status={statusLabel[order.status]}/></td><td>{new Intl.DateTimeFormat('en-NG',{dateStyle:'medium'}).format(new Date(order.created_at))}</td></tr>)}</tbody></table></div>}</Card></div>
}

function Metric({icon,label,value,financial=false}:{icon:ReactNode;label:string;value:string;financial?:boolean}){return <Card className="analytics-metric"><span className="analytics-metric-icon">{icon}</span><div><span>{label}</span><strong className={financial?'financial-value':''}>{value}</strong></div></Card>}
